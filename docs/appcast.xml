<?xml version="1.0" encoding="utf-8"?>
<!--
    Placeholder only. The Sparkle appcast is generated by the release workflow
    when a version tag like v26.4.0 is pushed or the workflow is run manually.

    A normal PR merge into main does not publish a release or regenerate this
    file. Until the first release runs, keep this placeholder instead of a
    valid-looking but empty RSS feed.
-->
